#include <stdio.h>

int main()
{
    int a = 512, b = 10, c;

    // c = 1; // skriver værdien 1 til variablen c
    // a++; // Tager den gamle værdi af a, og lægger 1 til.
    // a+=3; // Tager den gamle værdi af a, og lægger 3 til.
    // c = a+b; // lægger a sammen med b, og gemmer den i c
    // c = a==b // Sammenligner a med b (læs a lig med b), hvis de er det samme så ville c=1, ellers c=0
    // c = a!=b // Sammenligner a med b (læs a ikke lig med b), hvis de er det samme så ville c=0, ellers c=1
    // c = (a==b)? 13 : 55; // læs hvis det der står inde i () er sandt, så gem tallet 13 i c, ellers gem tallet 55
    return 0;
}
